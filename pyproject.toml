[build-system]
# Cap NumPy so manylinux builds install a wheel instead of compiling a
# source tarball that needs a newer GCC than manylinux2014 provides.
requires = ["setuptools>=61", "wheel", "Cython>=3.0", "numpy>=1.20,<2.5"]
build-backend = "setuptools.build_meta"

[project]
name = "sparc-pc"
version = "0.8.2"
description = "SparC: fast, modular sparse probabilistic circuits in Cython (CPU-only)"
readme = "README.md"
requires-python = ">=3.10,<3.15"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Adrian Ciotinga" }]
keywords = [
    "probabilistic-circuits",
    "sum-product-networks",
    "cython",
    "wasserstein",
    "machine-learning",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Cython",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "numpy>=1.20",
]

[project.optional-dependencies]
gurobi = ["gurobipy>=11.0"]
dev = [
    "build>=1.0",
    "pytest>=7.0",
    "pytest-randomly>=3.15",
    "scipy>=1.7",
    "twine>=4.0",
]
docs = [
    "Cython>=3.0",
    "numpy>=1.20",
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.5,<10",
    "mkdocstrings-python",
    "mkdocs-gen-files",
    "mkdocs-literate-nav",
    "mkdocs-section-index",
]

[project.urls]
Homepage = "https://github.com/aciotinga/sparc"
Documentation = "https://sparc-docs.readthedocs.io"
Repository = "https://github.com/aciotinga/sparc"
Issues = "https://github.com/aciotinga/sparc/issues"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["sparc*"]

[tool.setuptools.package-data]
"sparc" = ["*.pyx", "*.pxd", "*.pxi", "py.typed"]

[tool.cibuildwheel]
build = "cp310-* cp311-* cp312-* cp313-* cp314-*"
# Skip 32-bit: NumPy has no i686/win32 wheels for recent releases, so the
# wheel test install falls back to a source build that needs GCC >= 10.3.
skip = "*-musllinux_* *-manylinux_i686 *-win32 cp*t-*"
archs = ["auto64"]
# manylinux2014 ships GCC 10.2; recent NumPy wheels target 2_28+.
manylinux-x86_64-image = "manylinux_2_28"
manylinux-aarch64-image = "manylinux_2_28"
test-command = "python -c \"import sparc; from sparc import CategoricalInputNode, SumNode; print(sparc.__version__)\""

[tool.cibuildwheel.macos]
archs = ["x86_64", "arm64"]
# Homebrew libomp on macos-latest is bottled for the runner OS (currently
# macOS 26). delocate refuses to vendor that dylib into macosx_11_0 wheels.
# Ship serial compiled eval on macOS; local source builds can still set
# SPARC_OPENMP=1 against an older libomp.

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "gurobi: requires gurobipy and a valid Gurobi license",
    "gcw: GCW coupling / materialization tests (order- and platform-sensitive)",
    "gcw_stress: heap/order stress tests for GCW memoization isolation",
    "solver: direct unit tests for OT / assignment solvers",
    "metrics: ground metric and metric-driven query behavior",
    "eval: likelihood, sampling, and compiled batched evaluation",
    "optim: simplex_step, apply_grads, MLETrainer",
    "node: CircuitNode API (clone, save/load, normalization)",
    "queries: CW / GCW / exp / log_exp query behavior",
    "integration: cross-module leaf-type integration tests",
    "property: cross-cutting invariants (normalization, determinism)",
    "nodes: node construction and validation",
    "no_dual_path: opt out of object-graph vs compiled parametrization",
]
filterwarnings = [
    "error::pytest.PytestUnraisableExceptionWarning",
]
